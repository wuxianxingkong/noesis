(* Programa de ejemplo en BC *)

procedimiento Ejemplo;

inicio

  (* --- *)

  var
     indice, numero:      entero;
     iteraciones: entero;
     r,s:         entero;
     t,u:         booleano;
  fin;

  (* --- *)

  procedimiento Foo (var i, var j: entero; a,b: booleano);

  inicio

    var
       matriz: array[7..15, 3..8, 0..10];
    fin;

    procedimiento p1 (var a, b : entero);
    inicio
       var c : booleano;
       fin;

       procedimiento p2 (a, b : booleano);
       inicio
         var
            c : entero;
            d : array[5..10];
         fin;
          c := d[4];
          inicio
             var
                a : entero;
             fin;
             a := a;
          fin;
       fin;
       procedimiento p3 (m1 : array[0..5, 0..10]);
       inicio
          (* No hago nada *)
       fin;

       a := 56;
       p3(matriz[2]);
    fin;

    repetir
      i := j;
    hasta falso;        { Bucle infinito }

  fin;
  
  (* --- *)
  
  Foo (r,s,t,u);
  
  entrada iteraciones;

  salida 'N£mero de iteraciones = ';
  salida iteraciones;

  indice := 1;

  Mientras indice<=iteraciones hacer
    inicio

      entrada numero;

      Si (2 < numero) entonces salida 'N£mero par'
         si_no    salida 'Numero impar';

      numero := 3 + 45;
      indice := indice + 1; 

    fin;
  
fin.